export SLORA_IMAGE="${SLORA_IMAGE:-localhost/slora-sim:dev}"

if has podman && ! podman image exists "$SLORA_IMAGE" 2>/dev/null; then
    echo "[slora] image '$SLORA_IMAGE' not built yet; run \`just build\` to bake it"
fi
